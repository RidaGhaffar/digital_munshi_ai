apiVersion: v1
kind: Service
metadata:
  name: digitalmunshi-backend-service
spec:
  selector:
    app: digitalmunshi-backend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
  type: LoadBalancer
